<Page x:Class="sberbank.Pages.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Банковские услуги">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel>
            <StackPanel>
                <TextBlock Text="Банковские продукты и услуги" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="Выберите продукт и оставьте заявку на обслуживание" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,12"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <ComboBox x:Name="TypeComboBox" Width="190" SelectionChanged="FilterChanged"/>
                <CheckBox x:Name="SortCheckBox" Content="Сортировать по стоимости" VerticalAlignment="Center"
                          Margin="12,0,0,0" Checked="FilterChanged" Unchecked="FilterChanged"/>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProductsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="190"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding ProductType}" Foreground="{StaticResource PrimaryBrush}" Margin="0,4,0,8"/>
                                    <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding DisplayRate}" FontSize="16" FontWeight="SemiBold" TextAlignment="Right"/>
                                    <TextBlock Text="{Binding DisplayCost}" Foreground="{StaticResource MutedBrush}" TextAlignment="Right" Margin="0,4,0,16"/>
                                    <Button Content="Оставить заявку" Tag="{Binding ProductId}" Click="ApplyButton_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
