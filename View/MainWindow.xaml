<Window x:Class="sberbank.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ООО Сбербанк - личный кабинет"
        Height="720"
        Width="1120"
        MinHeight="560"
        MinWidth="860"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel Margin="18,0">
                <TextBlock Text="ООО Сбербанк" Foreground="{StaticResource PrimaryBrush}" FontSize="24"
                           FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Профиль" Click="ProfileButton_Click" Style="{StaticResource SecondaryButton}"/>
                    <Button Content="Услуги" Click="ProductsButton_Click" Style="{StaticResource SecondaryButton}"/>
                    <Button Content="Заявки" Click="ApplicationsButton_Click" Style="{StaticResource SecondaryButton}"/>
                    <Button Content="Выход" Click="LogoutButton_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
